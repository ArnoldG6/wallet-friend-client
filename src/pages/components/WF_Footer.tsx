import {Footer, Grid, createStyles} from "@mantine/core";

export default function WF_Footer() {
    /*
    const useStyles = createStyles((theme) => ({
        footer: {
            marginTop: 120,
            borderTop: `1px solid ${
                theme.colorScheme === 'dark' ? theme.colors.dark[5] : theme.colors.gray[2]
            }`,
        },
    })
    );
*/
    return (
        <Footer height={60}>
            holi
        </Footer>
    );
}